to do:

specify box url
learn about NAT & read about diffrent network types in VirtualBox
automatically apply VB guest addition (Vagrant plugin) - for shared folders

CRON is never a solution !! (for services / servers)


- - -

For later:

Use wordpress instead of flask for testing
